# 开发日志 · 2026-06-15

> Wave 1-3 实施期间的错误、未完成项、接续指引。
> 文件命名规则: `dev-log-YYYY-MM-DD.md` 每日一篇, 另存 `phase-delivery-*.md` 阶段性交付。

## 一、本日交付

- 18 法合参接通 + 5 维职责分派 (方案 §十三 / §二十一)
- 八字流年/紫微 4 化/西占行运当前周期能力
- 大六壬 9 宗门简化版课式判定
- 完整交付清单: `docs/phase-delivery-18methods-5dim.md`

## 二、遇到的错误 (已修)

| # | 错误 | 根因 | 修复 | 文件 |
|---|---|---|---|---|
| E1 | `tieban` 父母生肖校验 `or == 0` 永远放行 | 校验条件后短路 `or == 0` 几乎全命中 | 删短路, 严格 `% 1000 == expected_checksum` | `divination/engines/tieban.py:104-108` |
| E2 | `liuren._get_month_general` 12月越界 IndexError | 跨月查询用 `m % 12` 当 m=12 时取到大寒条目 | 改显式 if-elif 链 | `divination/engines/liuren.py:86-91` |
| E3 | `xiaoliuren/lenormand` 静默用 `date.today()` 降级种子 | `random.Random(seed or date.today())` | 改 `hashlib.sha256(seed).digest()` 派生, 无 seed 抛 `ValueError` | `xiaoliuren.py:91` / `lenormand.py:272` |
| E4 | `bazi_v2` selector 用了但 router 没注册 | selector 命名 `bazi_v2`, router 只有 `bazi` | router 加 `bazi_v2: bazi.compute` 别名 | `divination/router.py:11` |
| E5 | `fengshui` selector 用了但 router 没注册 | 同上, fengshui 是 selector 命名, bazhai/xuankong 是引擎 | router 加 `fengshui: bazhai.compute` 别名 | `divination/router.py:15` |
| E6 | `ziwei` 子进程 `Birth(**dict)` TypeError | `build_method_inputs` 注入 `question/subject/seed`, subprocess 用 `Birth(**dict)` 重建失败 → fallback chart | subprocess 代码改成字段白名单过滤 `_raw.items() if k in Birth.__dataclass_fields__` | `divination/engines/ziwei.py:200-210` |
| E7 | `western` `datetime.utcnow()` 不带 tz → skyfield 报错 | `datetime.utcnow()` 返回 naive datetime | 改 `datetime.now(timezone.utc)` | `divination/engines/western.py:103-104` |
| E8 | `tieban` `b.subject` 在 Birth 没设时崩 | 直接访问属性 | 改 `getattr(b, "subject", None) or "self_life"` | `divination/engines/tieban.py:187` |
| E9 | 老的 8000 端口 uvicorn 是改代码前启动, 不 reload | 用户侧进程管理 | 启新 8001 uvicorn + 改 `vite.config.ts` proxy → 8001 | `apps/web/vite.config.ts:10` |

## 三、已知但**没修**的瑕疵 (留着明天)

| # | 瑕疵 | 触发场景 | 当前行为 | 影响 |
|---|---|---|---|---|
| W1 | `decadal mutagen` 字段对 py_iztro 0.3+ 返回空字符串 `''` 而非 `[]` | `compute("ziwei", ...)` `raw['horoscope']['decadal']['mutagen']` 可能是 `['', '', '', '']` | 紫微 4 化 current_cycle signal 还是能出 (因为 normalizer 计数 'ji' 关键字, 4 个空字串 = 0 忌 4 吉 → 正向) | 不影响 report 渲染, 但说明 mutagens 解析对 py_iztro 老 API 不全 |
| W2 | `bazi shensha` 关键词过滤是硬编码 "贵/禄/福/文/天德" 等 | `_normalize_bazi` 写死中文关键词 | lunar-python 返回的神煞名称可能不全匹配 | 信号可能归为 fallback `general_reference` 而非 `noble_help/obstacle_pressure` |
| W3 | `bazi shensha` 抽取只用了日柱层 (getDayJiShen / getDayXiongSha), 没接年/月/时 | 简化 | 年柱月柱时柱的神煞未捕获 | 长期潜在信息缺一块 |
| W4 | `western.transits` 容许度是硬编码 8°/8°/6°/6°/4° (固定) | 简化 | 实际占星学按行星重要性区分 (冥王 10° / 木星 8° / 太阳 8°), 没用上 | 重大相位可能被漏掉 |
| W5 | `liuren 9 宗门` 是简化版, 完整版 30 个 golden case 待补 | Phase 3 风险 R2, 文献深 | 当前能判定 贼克/比用/涉害/遥克/昴星/伏吟/返吟/别责/八专 8 种 | 不影响演示, 但占星学界认 9 宗门全判 |
| W6 | `vedic Dasha` 当前周期 signal 未出 | Phase 3 范围 | 吠陀 Vimshottari 引擎已建, 但 normalizer 没读 Vimshottari Dasha 出 current_cycle signal | 5 维 current_cycle 缺吠陀贡献 |
| W7 | `selector.DIMENSION_CONFIG` 把 `bazi_v2/ziwei/western/vedic` 标在 `current_cycle` 维, 但 normalizer 里这些方法的 natal signal 也带 `dimension="long_term"` | 简化 | 同一方法在两个维里都出 signal (设计如此, call-once multi-tag) | validator 5 维评分会偏袒这些多维方法, 5 维 score 仅供参考 |
| W8 | `compute()['method']` 是 bazi, 但 `methods_used` 列表是 bazi_v2 (router alias 行为) | router 别名设计 | 18 法统计上 OK, 但 `chart.method` 与 `methods_used` 不一致, 可能影响调用方对信号归因的逻辑 | 极小, 报告/页面渲染无影响 |
| W9 | `synthesizer` 报告里"5 维职责分派"段后还跟了"5 维职责深度"段 (重复) | `_build_standard` 和 `_build_premium` 都加了 5 维分块 | 报告变长, 体验冗余 | 极小, 性能 OK, 可读性下降 |
| W10 | 报告里 "1 种术法" 用 hepan, 但 hepan 缺 partner 时是占位 error | 验证性错误 | hepan 在无 partner 时只能出 3 个 general_reference fallback, 显得"有" 1 种但实质失败 | 显示误导, 建议要么屏蔽 hepan 要么显式标注"未提供 partner" |

## 四、未开发 (按优先级, 明天起手)

### A. 现实条件校正引擎 (方案 §十四) — 高 ROI 差异化
- 没建任何 `RealityConstraintEngine`, 报告里"命理层说适合" / "现实层说现金不足" 这类接地气结论缺
- 缺口:
  - 现金储备判断 (基于 `request.constraints.cash_reserve_months`)
  - 合同 vs 口头 offer 区分
  - 通勤地点
  - 健康状态
  - 资质限制
- 预计工作量: 1-2 天, 1 个新模块 `divination/aggregation/reality.py` + 接 reading_service

### B. Phase 2 时辰校正前端 (缺口, 已有后端)
- `server/api/birth_time.py` 完整, 4 种精度模式 + 候选评分 + 追问建议
- **没有前端页面** (`BirthTimeRectifyPage.tsx` 未建)
- 预计工作量: 半天, 1 个新页面 + 路由 + 接入 Cases 流程

### C. Phase 3 RandomService (方案 §十一) — 部分合规
- ✅ xiaoliuren / lenormand 已改用 hashlib
- ❌ 蓍草筮法流程未实现 (方案 §十一 蓍草专用概率: 老阳 3/16, 少阳 5/16, 少阴 7/16, 老阴 1/16)
- ❌ seed hash / 算法版本号还没统一加到所有引擎的 raw 输出
- 预计工作量: 1 天

### D. Phase 4 罗盘 + 户型图 (方案 §九)
- 已有 `apps/web/src/components/CompassDial.tsx` 组件
- ❌ 缺 API 端点 (测量保存/24 山转换/连续采样)
- ❌ 缺前端罗盘采集页
- 预计工作量: 1-2 天

### E. Phase 5 古籍知识库 (方案 §十六)
- `divination/knowledge/` 已有 `professions.py domains.py classical.py` 等, 但未结构化接入报告
- 报告里没有"古籍来源引用"功能
- 预计工作量: 2-3 天

### F. Phase 6 复盘与长期数据 (方案 §六-7)
- 完全没建
- 缺 `POST /api/cases/{id}/review` 端点
- 缺历史档案的版本对比 UI
- 预计工作量: 2-3 天

### G. 大六壬 9 宗门完整 30 golden case
- 6 个 case 的简化测试已过 (test_liuren_pattern_judgment)
- 完整文献案例 (《大六壬探源》/《六壬大全》/《六壬指南》) 待补
- 预计工作量: 1 天

### H. 西占 progressed / secondary progression
- 当前只有 transits (本命 vs 行运行星)
- 占星学还常用次限推运 (secondary progressions, 出生后第 N 天 = N 岁时的星图)
- py_iztro / skyfield 都可算, 但当前没实现
- 预计工作量: 1-2 天

## 五、文件位置速查 (明天接续看这些)

### 新建/改过的核心文件
- 引擎层: `divination/engines/{__init__.py, tieban.py, liuren.py, xiaoliuren.py, lenormand.py, bazi.py, ziwei.py, western.py}`
- 聚合层: `divination/router.py`, `divination/aggregation/{schema.py, selector.py, method_inputs.py, normalizer.py, validator.py, synthesizer.py, reading_service.py}`
- 前端: `apps/web/src/lib/types.ts`, `apps/web/src/components/ReadingReportView.tsx`, `apps/web/vite.config.ts`
- 测试: `tests/{test_selector, test_normalizer, test_reading_service, test_synthesizer, test_api, test_golden_classics, test_cases_api, test_birth_time_api}.py`
- 文档: `docs/phase-delivery-18methods-5dim.md`, `docs/phase1-case-flow.md`, `docs/phase2-birth-time-rectification.md`, **`docs/dev-log-2026-06-15.md` (本文)**

### 进程 / 端口
- 用户 uvicorn: 8000 (旧, 进程 PID 52296, 161MB) — **明天杀掉换成 8001 的代码, 或保留 8001 把 8000 指向新代码**
- 我的测试 uvicorn: 8001 (新, 含所有今日修改)
- 前端 vite dev: 5173 (proxy 已改 → 8001, 明天可能需要重启 vite)
- 浏览器记忆: 用过 `case_9a6a5ae44a24` 这种 case_id 都已 in-memory 清空

### 测试状态
- **455 passed** (今日 454 → 加 1 个 liuren 9 宗门 golden test)
- 跑测试: `.venv/Scripts/python.exe -m pytest tests/ --no-header -q`

## 六、明天接续建议 (按你时间)

- **时间少 (2-3 小时)**: B 方向 (Phase 2 时辰校正前端, 性价比最高)
- **时间中 (半天-1 天)**: A 方向 (现实条件校正引擎, 差异化亮点) 或 C 方向 (独立 RandomService + 蓍草)
- **时间多 (1+ 天)**: D 方向 (罗盘 + 户型图) 或 修 W 瑕疵 (W1-W10) + 写 Pydantic test coverage

## 七、注意事项

- **方案 §十一 AI 不参与随机**: 已经覆盖了 xiaoliuren/lenormand, 但还有 3 处 `random` 使用风险待审: `divination/router.py:91-92` (calibrate_birth_hour/estimate_hour_from_traits), `divination/engines/chenggu.py` (查), `divination/engines/numerology.py` (查). 明天可以 grep 一次全量审.
- **方案 §三 "新建 4 法 golden 测试通过方可进入会审"**: liuren/xiaoliuren/tieban/lenormand 都有 happy path 测试, 但 30 个文献黄金 case 覆盖率仍低.
- **方案 §六 复盘**: 完全空缺, 是长期数据壁垒, 但工作量 2-3 天.

---

## 附：2026-06-16 引擎优化完成项

> 今日实施：W2/W3/W4/W6/W8/W9/W10 + Phase A/B + Phase 2 前端

### 已修复的瑕疵

| # | 修复内容 | 改动文件 |
|---|---------|---------|
| W2 | bazi 神煞关键词硬编码改为 lunar-python 全量返回值接受 | `normalizer.py` |
| W3 | 神煞从日柱扩展到 ShiShen 十神四柱（年/月/日/时）+ lunar-python 年月时吉神/凶神全量接入 | `baz.py` + `normalizer.py` |
| W4 | western transits 容许度按行星重要性分级（冥王10°/木土8°/太阳8°/火星6°/金星5°/水星4°） | `western.py` |
| W6 | vedic Vimshottari Dasha current_cycle signal 输出，normalizer 读取大运主星+庙旺落陷判断吉凶 | `normalizer.py` |
| W8 | router compute() 返回请求名而非注册名，`chart.method == methods_used` 一致 | `router.py` |
| W9 | _build_standard 重复的"五维职责深度"段删除，保留一份 | `synthesizer.py` |
| W10 | hepan 无 partner 时从 effective_methods 排除，不混入 18 法统计；reading_service 加 `hepan_no_partner` 字段 | `reading_service.py` + `schema.py` |

### Phase B：现实条件校正引擎（方案 §十四）

- `divination/aggregation/reality.py` **新建** — `RealityConstraintEngine`（现金/合同/搬迁/健康/资质/家庭/备选 7 维）
- `schema.py` 加 `RealityConstraints` 输入 + `reality_adjusted` API 响应字段
- `synthesizer.py` 标准/高级版报告加现实条件段落（警告+调整后建议）
- `reading_service.py` 接入引擎
- **455 tests pass**

### Phase 2 前端（时辰校正页面）

- `BirthTimeRectifyPage.tsx` **新建**（4 精度模式+人生事件+候选时辰排名）
- `App.tsx` 加 `/birth-time` 路由
- `Cases.tsx` 加未校正引导 banner
- `birth.ts` 加 `birth_time_accuracy` 字段
- `api.ts` 加 `rectifyBirthTime()` + 类型

### 古籍规则库增强

- `classical.py` 28 → 55 条规则，新增 `passage` 古籍原文字段
- 大六壬指南9宗门全量规则（含原文）
- 渊海子平/滴天髓/三命通会 扩充至各 7-14 条
- `extract_rules_for_chart()` 新增大六壬/六爻/十神心性关键词分类
- 455 tests pass

### liuren 九宗门修复

- 比用法：找与日干五行相同上神（非简单取第一课）
- 涉害法：地盘克方最多（涉害最深）者
- 遥克法：取日干寄宫
- 补充 `day_gan` 到 `_build_four_lessons` 返回值
- 455 tests pass

### 测试状态

```
455 passed in 9.69s
```
