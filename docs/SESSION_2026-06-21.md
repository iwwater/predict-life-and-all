# Session Handoff — 2026-06-21

> 接续 6.21 审查报告的修复工作。
> 目标：消除“文档与代码不一致”与“六神功能测试被 skip”的真实风险。

---

## 一、本次完成

### 1.1 六爻六神测试解 skip

| 项 | 结果 |
|---|---|
| `tests/test_liuyao_six_shen_fu_shen.py` | 移除整文件 skip |
| `divination/engines/liuyao.py` | 补齐六神逐爻注解、动变兼容字段、世应六合/六冲判断 |
| 回归 | `43 passed` |

关键变化：

- `raw["六神注解"]` 从仅按六神名映射，升级为 6 条逐爻注解，包含 `爻 / 六神 / 主事 / 吉凶 / 适用`。
- `_transform_lines()` 保留原有 `原爻 / 变爻 / 变换`，新增 `状态 / 变化 / 动爻`，用于前端与报告层稳定消费。
- `_shiying_relation()` 在五行生克外补充地支六合/六冲提示。

### 1.2 文档同步

| 文件 | 处理 |
|---|---|
| `README.md` | 测试数、当前 handoff、TODO 状态更新到 6.21 |
| `docs/SESSION_2026-06-20.md` | 标注为历史快照，避免继续误导 |
| `docs/INDEX.md` | 更新当前 handoff 指向 6.21 |

### 1.3 小清理

| 文件 | 处理 |
|---|---|
| `divination/astro_math.py` | 移除 `houses()` 中 “Placidus 待实现” 的误导性分支说明；明确 Placidus 应调用 `placidus_houses()` |
| `tmp/` | 当前工作区未发现 `tmp` 目录，无需清理 |
| `setup.py` | 已纳入提交 `d9314bb`（rebase 后哈希），用于包安装元数据 |

### 1.4 Track 2 数据与算法状态同步

| 项 | 处理 |
|---|---|
| 称骨 | 51 档主表 source 从 `partial` 统一为 `印本《称骨歌》清刻本（51档校订）`，新增 source 非 partial 回归 |
| 铁板 | 现有太玄数精校 / 纳音 / 分金 / 双流派能力已存在；新增双流派端到端条文范围差异回归 |

### 1.5 塔罗三系统融合

| 项 | 处理 |
|---|---|
| engine | `tarot_system=waite/thoth/modern`，每张牌返回 `三系统解读` 与 `主体系解读` |
| API | `/api/compute` 放行 `tarot_system`，并新增回归测试 |
| 前端 | `/cast`、`/m/tarot`、`ReadingForm` 均可选择韦特 / 托特 / 现代心理 |
| 结果展示 | `TarotChart` 兼容中文 raw 字段，展示体系名与主体系解读 |

### 1.6 观音/关帝灵签产品化

| 项 | 处理 |
|---|---|
| engine | 新增 `divination/engines/qian.py`，支持 `guanyin/guandi`、随机抽签、seed 复现、手动签号 |
| API | `qian` 注册到 `_ENGINES` / `METHOD_META` / `/api/compute`，放行 `qian_type`、`qian_number` |
| 合参 | selector / method_inputs / normalizer 纳入 `qian`，作为 one_question 短期问事信号 |
| 前端 | 新增 `/m/qian`，首页 fallback、类型、结果图表、分享标签均接入 |
| 资料等级 | 1-25 手工条目标记 `curated`，26-100 基础补全标记 `base_catalog`，不伪装精校全文 |

---

## 二、验证记录

```bash
python -m pytest tests/test_liuyao_six_shen_fu_shen.py -q
# 43 passed

python -m pytest tests/test_liuyao_deep.py -q
# 27 passed

python -m pytest tests/test_chenggu_verses.py tests/test_tieban_deep.py tests/test_tieban_verses.py -q
# 86 passed

python -m pytest tests/ -q
# 2150 passed in 164.82s

cd apps/web && cmd /c "npx tsc --noEmit"
# passed

cd apps/web && cmd /c "npm run build"
# passed

python -m pytest tests/test_api.py -k tarot -q
# 7 passed

python -m pytest tests/test_api.py tests/test_selector.py tests/test_normalizer.py -q
# 200 passed
```

备注：第一次全量测试曾出现 1 次紫微飞星 `enriched.natal` 空结果；该单测独立复跑通过，第二次全量复跑通过。未改紫微逻辑，暂按偶发顺序/子进程状态问题记录观察。

---

## 三、仍然真实存在的后续缺口

- 古籍知识库 RAG 与文献出处面板：知识 API/书单已成型，引用语料与前端展示仍待产品化。

## 四、Git 同步记录

| 项 | 结果 |
|---|---|
| 稳定化提交 | `d9314bb Stabilize divination regression coverage` |
| 远端冲突处理 | `origin/master` 曾删除 `前端重设计样张v2.html`；已通过 `93ca7d8 Restore frontend redesign sample` 显式恢复 |
| 推送 | `git push origin master` 成功，远端已到 `93ca7d8` |

---

*文档生成: 2026-06-21 · 六神测试解 skip + 文档同步*
