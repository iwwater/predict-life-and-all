# 玄学平台 · 前端设计计划书（交付本地 Agent 实现）

> 目标读者：本地编码 Agent。本文是**可直接执行的实现规格**，非泛泛建议。
> 后端已就绪：12 法统一排盘 + 断法层 + 可插拔 LLM 解读层。前端只需把它"穿衣服 + 给入口"。
> 凡标 **【硬约束】** 的不可改动；标 **【可自由发挥】** 的 Agent 可自行设计。

---

## 0. 产品一句话

一个中西融通的玄学工具：用户输入出生信息（及问题），选择一种或多种术数，平台排盘并由 LLM 给出克制、负责的解读。**定位是"传统文化与自我反思工具"，不是科学预测**——这条贯穿全站文案与合规。

支持 12 法：
- 东方·命：八字 `bazi`、紫微 `ziwei`
- 东方·卜式：奇门 `qimen`、六爻 `liuyao`、梅花 `meihua`、称骨 `chenggu`
- 东方·风水：八宅 `bazhai`、玄空飞星 `xuankong`
- 西方：占星 `western`、吠陀 `vedic`、塔罗 `tarot`、数字命理 `numerology`

---

## 1. 技术栈【可自由发挥，给推荐】

- **React 18 + TypeScript + Vite**
- **Tailwind CSS**（设计 token 见 §7）+ shadcn/ui 作基础组件
- 状态：**TanStack Query**（服务端数据/缓存）+ 轻量 **Zustand**（表单与会话态）
- 路由：React Router
- 图表/盘面：**SVG 自绘**为主（星盘圆图、九宫格、四柱表都用 SVG，便于精确与主题化）；
  西方星盘可选 `@astrodraw/astrochart` 类库，但建议自绘以统一视觉
- 流式解读：**SSE / fetch ReadableStream** 读取后端流
- 动画：Framer Motion（摇卦、翻牌、飞星过渡）

---

## 2. 前后端架构与 API 契约【硬约束：契约形状固定】

后端需暴露一层 REST（FastAPI 包 `divination.router.compute` 与 `divination.interpret.interpret`）。前端按此契约开发；若后端未就绪，先按本契约 mock。

### 2.1 `GET /api/methods`
返回方法元数据，用于动态渲染方法选择与各自的额外参数表单。
```json
[
  {"id":"bazi","name":"八字","group":"命","needs":["birth"]},
  {"id":"xuankong","name":"玄空飞星","group":"风水","needs":["period","sitting"]},
  {"id":"tarot","name":"塔罗","group":"西方","needs":["spread","question"]},
  {"id":"liuyao","name":"六爻","group":"卜","needs":["question"]}
  // …其余 8 法，needs 仅含 ["birth"]
]
```

### 2.2 `POST /api/compute`
```json
// 请求
{
  "method": "bazi",
  "birth": {"year":1990,"month":5,"day":15,"hour":8,"minute":30,
            "gender":"male","calendar":"gregorian",
            "lat":31.23,"lng":121.47,"tz":"Asia/Shanghai","is_leap_month":false},
  "options": {"period":9,"sitting":"子","spread":"three","seed":null,"query":"事业"}
}
// 响应 = ChartResult（见 §3）
```

### 2.3 `POST /api/interpret`（**流式**）
```json
// 请求
{"charts": [ChartResult, ...], "question": "今年事业如何？"}
// 响应：SSE 流，data 块为解读文本增量；结束帧附 meta
// {"type":"delta","text":"……"}  多次
// {"type":"done","meta":{"softened_terms":["注定"],"methods":["bazi","western"],"blocked":false}}
// 若 blocked=true（危机话题）：仅返回一条转介文案，不出解读
```

### 2.4 `GET/POST /api/history`（可选，登录后）
保存/读取历史排盘与解读。未登录用 IndexedDB 本地存。**不要用 localStorage 存大对象**。

---

## 3. 统一数据结构 ChartResult【硬约束：按此渲染】

```ts
interface ChartResult {
  method: string;            // 上述 12 个 id 之一
  school: "east" | "west";
  engine: string;
  normalized: {              // 跨法通用，给概览/雷达图
    elements: Record<string, number>;   // 东方=五行(metal/wood/...)，西方=四元素(火/土/风/水)
    timeline: {from:string; to:string; label:string; score:number|null}[];
  };
  raw: Record<string, any>;  // 各法专属，结构见 §5 各法
}
```
> `normalized.elements` 用于全站统一的"五行/元素强弱雷达图"组件；`raw` 决定各法专属盘面。

---

## 4. 页面与路由【硬约束：流程；可自由发挥：布局】

```
/                首页：定位说明 + "开始排盘" + 12 法卡片网格
/cast            排盘入口：① 出生信息表单 ② 选术数(可多选) ③ 各法额外参数 ④ 问题(可选)
/result/:id      结果页：左=盘面可视化(按所选法 Tab/堆叠)；右=解读面板(流式)
/methods/:id     单法说明页：该术数是什么、怎么读、文献出处(取自 文献依据.md)
/history         历史记录(可选)
/about           关于与免责声明全文
```

### 关键流程
1. `/cast` 收集 `birth` + 选中的 methods + 各法 options + question。
2. 对每个选中 method 调 `POST /api/compute` → 得到多个 ChartResult。
3. 跳 `/result/:id`，左侧渲染各盘面，右侧用这些 charts 调 `POST /api/interpret` **流式**展示解读。
4. 多法时解读为"中西合参"，UI 上不要并列成相互竞争的结论，而是一段融合解读 + 可展开看各法原盘。

---

## 5. 各术数盘面可视化规格【核心工作量，逐法给清楚】

每个盘面是一个独立组件，输入 `raw`。**【可自由发挥】美术细节，**【硬约束】**列出的盘面元素必须呈现且正确对位。**

### 5.1 八字 `bazi`
- 四柱表：年/月/日/时 四列，每列上干下支；标注日主、十神（若有）。
- 五行强弱：用 `normalized.elements` 画雷达图或条形；标"最旺/缺"。
- 大运时间轴：`normalized.timeline` 横向时间轴。

### 5.2 紫微 `ziwei`
- **十二宫方形排盘**（传统紫微盘是 4×4 去中心的 12 宫环）。`raw.palaces[]` 每宫含 `name`、`major_stars[]`、`is_body`。
- 标命主/身主/五行局。命宫高亮。

### 5.3 奇门 `qimen`
- **九宫格 3×3**（按后天八卦方位：巽离坤/震中兑/艮坎乾）。每宫叠放：天盘干、地盘干、九星、八门、八神（`raw.天盘三奇六仪/地盘三奇六仪/九星/八门/八神`，均为 {宫:值} 字典）。
- 顶部条：`raw.排局`、`节气`、值符值使、旬空。
- `raw.断` 的格局/门迫/空亡宫，用角标或高亮标注对应宫。

### 5.4 六爻 `liuyao`
- **六爻竖排**（自下而上初爻→上爻）。每爻：阴/阳爻象（▬▬ / ▬ ▬）、纳甲地支+六亲+六神、标"世/应"、动爻标 ○/×。
- 本卦 → 变卦 并列；`raw.断` 的用神、旬空、月破在对应爻上角标。
- 摇卦交互见 §6。

### 5.5 梅花 `meihua`
- 主卦/互卦/变卦 三个六爻卦象并列；标体卦/用卦；`raw.断.总断` 给吉凶徽标。

### 5.6 称骨 `chenggu`
- 一个"秤"视觉：年月日时各骨重 + 总骨重；下方展示批语。

### 5.7 八宅 `bazhai`
- **八方罗盘图**（八卦方位八格）。每方位标游年星名 + 吉/凶配色；命卦居中；标东四/西四命。

### 5.8 玄空飞星 `xuankong`
- **九宫格**，每宫显示三个数：运星/山星/向星（`raw.九宫` = {卦:{运,山,向}}）。坐/向用箭头标注；`raw.格局`给徽标（旺山旺向/上山下水/双星到向…）。

### 5.9 西方占星 `western`
- **圆形星盘**：黄道十二宫环、12 宫位、行星按 `raw.planets[name].lon` 落位、上升/天顶轴线、相位连线（`raw.aspects`，不同相位不同颜色：合/拱/刑/冲/六分）。
- Placidus 宫位线用 `raw.houses[].cusp_lon`。

### 5.10 吠陀 `vedic`
- 可用**北印度方形盘**或南印度盘；每宫标 Rashi + 落入行星 + Nakshatra/Pada；顶部标 ayanamsa。

### 5.11 塔罗 `tarot`
- 按 `raw.牌阵`（single/three/celtic）布局牌位；每张牌：牌名、正/逆位（逆位牌面旋转 180°）、关键词。翻牌动画见 §6。

### 5.12 数字命理 `numerology`
- 生命灵数/命运数大字展示 + 释义；简洁卡片即可。

---

## 6. 关键交互细节【可自由发挥，给方向】

- **六爻摇卦**：进结果前可有"摇三枚铜钱 × 6"的动画交互；用户点击摇动，前端生成 6 个 tosses 传给 `options.tosses`（或后端随机+seed）。让"亲手摇出"有仪式感。
- **塔罗翻牌**：洗牌动画 → 用户点选牌位 → 翻面（Framer Motion flip）。
- **风水坐向输入**：玄空/八宅的 `sitting` 用一个**罗盘选择器**（24 山转盘）而非下拉，体验更对味。
- **流式解读**：右侧解读逐字/逐段流入；顶部小字标"解读生成中"。
- **状态**：每个盘面与解读都要有 loading（骨架屏）/ empty / error 三态。解读 error 给"重试"。

---

## 7. 视觉设计系统【硬约束：方向；可自由发挥：细节】

**调性：克制的神秘感 + 专业可信。避开塔罗摊俗气路线**（不要紫色霓虹、不要水晶球 emoji 堆砌）。参考"天文馆 / 高端命理顾问"的质感。

```css
/* 暗色为主（夜空），亮色可选 */
--bg:        #0E1117;   /* 近黑夜空蓝 */
--surface:   #161B22;
--ink:       #E6E1D3;   /* 暖白文字 */
--muted:     #8A8F98;
--gold:      #C9A24B;   /* 主点缀：星图金 */
--jade:      #4FB3A0;   /* 东方·青 */
--azure:     #5B8DEF;   /* 西方·蓝 */
--danger:    #C8553D;   /* 凶/警示 */
--ok:        #5AA469;   /* 吉 */
```
- **东方法用 `--jade` 系，西方法用 `--azure` 系**，中西合参页同时出现时以此区分来源。
- 字体：标题用一款有古典气质的衬线（思源宋体 / Noto Serif SC）；正文无衬线（思源黑体 / Inter）。
- 盘面线条精细、留白充足；吉凶用色克制（不要满屏红绿）。
- 全站**深色优先**，移动端响应式（盘面在窄屏可横向滚动或缩放）。

---

## 8. 合规与福祉【硬约束·不可省】

1. **免责声明常驻**：每个解读结果底部必须显示后端返回的免责文案；首页与 /about 有完整版。文案口径："传统文化象征视角的参考，非科学预测"。
2. **危机处理**：当 `interpret` 返回 `blocked:true`，**只展示转介文案**（如心理援助提示），**不得**渲染任何解读或盘面吉凶，不得诱导继续追问。
3. **不放大焦虑**：UI 不用"大凶""死劫"等惊吓式标签；凶用中性措辞（"需留意""阻力"）。
4. **医疗/法律/财务**：解读里若带这类转介提示（后端 notes），原样显示，不弱化。
5. 不显示广告、不诱导成瘾式连续抽卦（如设每日次数提示）。

---

## 9. 建议交付顺序（给 Agent 的里程碑）

1. **骨架**：路由、设计 token、API client（先 mock §2 契约）、ChartResult 类型。
2. **排盘流**：`/cast` 表单（出生信息 + 方法多选 + 动态额外参数）→ compute → `/result`。
3. **盘面组件**：先做八字、西方占星、奇门、玄空（覆盖四种典型形态：表/圆盘/九宫/飞星），跑通后补其余。
4. **解读面板**：接 `/api/interpret` 流式 + 三态 + 免责 + 危机分支。
5. **特殊交互**：摇卦、翻牌、罗盘选择器。
6. **历史/账户**（可选）、移动端打磨、暗亮主题。

---

## 10. 对 Agent 的硬约束清单（务必遵守）

- **不要**改动 §2 API 契约形状与 §3 ChartResult 结构（前后端据此对接）。
- **不要**用 localStorage 存大对象（用 IndexedDB / 后端）。
- **不要**把任何术数包装成"科学/医学/投资建议"；§8 合规项逐条落实。
- **不要**在 `blocked:true` 时渲染解读或盘面。
- **盘面对位必须正确**：九宫方位、四柱干支、爻序自下而上、星盘行星落度——这些是"对不对"的红线，宁可样式简单也不能排错。
- 盘面数据**只来自 `raw`/`normalized`**，不要前端自行推算命理结论（推算在后端）。
- 文案中文为主，措辞遵循 §8 口径。

---

## 附：本地启动联调

- 后端：`pip install -r requirements.txt`，用 FastAPI 包 `divination.router.compute` 与 `divination.interpret.interpret` 暴露 §2 端点（解读端点接真实 LLM 需配 Key；联调可用 MockClient）。
- 星历：`skyfield-data` 已含离线 `de421.bsp`，生产打进镜像，勿运行时下载。
- 前端 `.env`：`VITE_API_BASE=http://localhost:8000/api`。
