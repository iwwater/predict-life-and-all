# 开发方案 · P1 化解层 + P2 风水平面图（实现级规格）

> 性质：**实现级开发方案**，模块/数据结构/算法/API/验收全部落实，可直接照此编码。
> 依赖现状：12 法引擎(已验证) + 断法层 + 解读层(prompts/guardrails/client) + Mystic Hub 服务壳。
> ⚠️ 全局红线（化解不恐吓、不带货、不替代专业、象征性定位）贯穿两期，验收项里逐条体现。

---

# 第一部分 · P1 化解层（从"算"到"改"）

## 1.1 模块结构

```
divination/remedy/
├── __init__.py          # 导出 prescribe()
├── knowledge.py         # 化解知识库（纯数据，每条带文献出处）
├── engine.py            # 诊断→处方匹配引擎
└── priority.py          # 处方排序与红线过滤
```

## 1.2 知识库数据结构（knowledge.py）

每条化解是一个 `Remedy` 记录。**【硬约束】字段齐全，`source` 必填：**

```python
@dataclass
class Remedy:
    id: str                # "fs_star2_metal_drain"
    domain: str            # wuxing | flying_star | sha | bazhai | bazi | qimen | liuyao
    trigger: dict          # 触发条件（与诊断输出字段对齐，见 1.3）
    action: str            # 化解动作（人话，一句）
    mechanism: str         # 玄学机理（"金泄土气"——给解读层引用）
    cost: str              # free | low | medium   ← 不设 high，红线
    category: str          # behavior | placement | color | object | timing
    source: str            # 文献出处（"《沈氏玄空学》卷X·二黑病符" 类）
    caution: str = ""      # 注意事项（"忌红黄色助凶"）
```

### 知识库四张核心表（数据起点，编码时按文献逐条校）

**A. 五行调理 `WUXING`**（接八字喜用神/缺失五行）
| 补 | 方位 | 色 | 物(low-cost) | 行为(free) |
|----|------|----|----|----|
| 木 | 东/东南 | 绿青 | 绿植、木质摆件 | 多晨间活动、亲近园林 |
| 火 | 南 | 红紫橙 | 暖光灯、烛形灯 | 多晒太阳、社交活跃 |
| 土 | 中/西南/东北 | 黄棕 | 陶瓷、石件 | 规律作息、稳定居所 |
| 金 | 西/西北 | 白金银 | 铜器、金属件 | 整理收纳、立规矩 |
| 水 | 北 | 黑蓝 | 小水景、流线摆件 | 近水散步、多学习 |

**B. 玄空九星 `FLYING_STAR`**（接 xuankong 各宫飞星）
| 星 | 性 | 凶时化解(机理) | 吉时催旺 |
|----|----|----|----|
| 一白 | 吉·桃花文昌 | — | 见水催之 |
| 二黑 | 凶·病符 | **金泄土**：铜铃/金属钟；忌红黄(火土助凶) | — |
| 三碧 | 凶·是非 | **火泄木**：红色物；慎金克(激其性) | — |
| 四绿 | 吉·文昌 | — | 绿植/文昌位置书桌 |
| 五黄 | 大凶·廉贞 | **金泄**：铜葫芦/六铜钱；**忌动土、忌红黄** | — |
| 六白 | 吉·武曲 | — | 金属饰、利权位 |
| 七赤 | 凶·破军 | **水泄金**：蓝黑色、静水 | — |
| 八白 | 大吉·当运财 | — | 宜动、见水、明亮 |
| 九紫 | 吉·喜庆 | — | 暖色催喜 |

**C. 形势煞 `SHA`**（接平面图检测，P2 用）
| 煞 | 判定(可计算) | 化解(分级) |
|----|----|----|
| 穿堂煞 | 大门-对侧门/窗连线无遮挡 | free:家具改动线 / low:屏风、玄关帘 |
| 门对门 | 两门中线夹角<15°且相对 | free:常闭一门 / low:门帘 |
| 开门见灶/厕 | 入门视线扇区含灶/厕 | free:改门内动线 / low:屏风 |
| 厕居中宫 | 卫生间质心落中宫格 | free:常通风保洁 / low:绿植、海盐 |
| 横梁压顶 | 用户标注梁线过床/桌/灶 | free:移床位 / medium:吊顶包梁(提示咨询设计) |
| 缺角 | 该卦位扇区面积<全宅1/9×0.6 | low:按缺角卦五行补(查表A) |

**D. 八宅归位 `BAZHAI_PLACE`**（接 bazhai 命卦四吉凶方）
- 床头/书桌/灶口(火门朝向) 宜四吉方(生气/天医/延年/伏位)，厕/储压凶方为宜。
- 不合时化解：free=调整朝向/移位；low=屏风改气口。

## 1.3 处方引擎（engine.py）

```python
def prescribe(charts: list[ChartResult],
              floorplan: FloorplanDiag | None = None) -> list[Prescription]:
    """诊断 → 匹配知识库 → 排序过滤 → 处方清单"""
```
- **输入诊断源**：`bazi.raw.断`(缺失/最旺五行)、`xuankong.raw.九宫`、`bazhai.raw.八方`、
  `qimen.raw.断`、`liuyao.raw.断`、P2 的平面图诊断。
- **匹配**：trigger 是声明式条件（如 `{"domain":"flying_star","star":5,"room_type":"bedroom"}`），引擎按字段匹配。
- **冲突仲裁【硬约束】**：空间问题以风水盘为准，命主调理以八字喜用为准；同一空间飞星与八宅结论冲突 → **并陈两派**（输出两条各标 source），不合成。

## 1.4 排序与红线（priority.py）

```python
ORDER = {"behavior":0, "placement":1, "color":2, "object":3, "timing":4}
COST  = {"free":0, "low":1, "medium":2}
# 排序键 = (COST, ORDER)；输出每类最多 N 条（默认总共 ≤6 条，防化解轰炸）
```
- **红线过滤**：扫描 action/mechanism 文本，命中禁词（"必须购买/开光/法器/消灾/否则…"）直接 drop 并告警日志。
- medium 成本项必须附 "建议咨询专业设计/人士"。

## 1.5 解读层接入

- `interpret/prompts.py` 增 `REMEDY_SECTION`：处方序列化为
  `[针对] 问题 → 化解(机理，出处) → 注意`，注入 user prompt 末段；
  系统提示补一句："化解建议仅可来自下方处方清单，不得自创化解或加码恐吓"。
- `guardrails.soften_output` 对含化解的输出照常生效；新增禁词表（1.4）同时挂到输出侧。

## 1.6 API

```
POST /api/remedy
{ "charts":[ChartResult...], "floorplan_diag": FloorplanDiag|null }
→ { "prescriptions":[{problem, action, mechanism, cost, category, source, caution}...] }
```
（/api/interpret 可带 `include_remedy:true` 一次出"解读+调理建议"。）

## 1.7 P1 验收清单

- [ ] 知识库每条 `source` 非空；抽查 10 条与文献一致
- [ ] 八字缺木 → 输出含东方位/绿色/绿植/晨间活动，free 项排最前
- [ ] 玄空五黄到卧室 → 金泄类化解 + "忌动土忌红黄" caution；**无**任何购买高价物建议
- [ ] 飞星 vs 八宅冲突用例 → 两派并陈各带 source，不合成单一结论
- [ ] 禁词用例（"不化解必破财""请购开光葫芦"）→ 被过滤，日志告警
- [ ] 处方总数 ≤6；medium 项 100% 带"咨询专业"提示
- [ ] 解读输出含化解时，免责声明照常追加

---

# 第二部分 · P2 风水平面图（空间化诊断 + 图上化解）

## 2.1 模块结构

```
divination/floorplan/
├── __init__.py       # diagnose()
├── models.py         # Floorplan/Room/Door 数据模型
├── geometry.py       # 质心/扇区归属/缺角/视线（纯几何，可单测）
├── overlay.py        # 九宫·二十四山叠加（接 xuankong/bazhai）
├── sha_detect.py     # 形势煞规则检测
└── report.py         # 诊断汇总 → FloorplanDiag（喂 P1 prescribe + LLM）
```

## 2.2 数据模型（models.py）【硬约束：前后端共此契约】

```ts
interface Floorplan {
  outline: [number,number][];        // 外轮廓多边形(顺时针)，单位任意(归一化)
  facing_deg: number;                // 朝向：大门朝向方位角(0=正北,顺时针)
  facing_tolerance: number;          // 默认5(°)
  rooms: Room[];
  doors: Door[];                     // 至少含大门 type:"main"
  beams?: [ [number,number],[number,number] ][];  // 用户标注的梁线段
}
interface Room {
  id: string;
  type: "bedroom"|"master_bedroom"|"kitchen"|"stove"|"bathroom"|
        "living"|"study"|"storage"|"balcony"|"entry";
  polygon: [number,number][];
  bed_head_deg?: number;             // 卧室床头朝向(可选)
  stove_mouth_deg?: number;          // 灶口朝向(可选)
}
interface Door { id:string; type:"main"|"room"; position:[number,number]; facing_deg:number; room_id?:string }
```

## 2.3 几何算法（geometry.py）——每个函数独立单测

1. **太极点**：外轮廓多边形质心（shoelace 公式）。凹形宅提示"质心若落墙外，建议手动微调太极点"。
2. **宫位归属（扇区法）【硬约束用扇区法，不用九宫格】**：
   以太极点为圆心，按 `facing_deg` 旋转后，360° 分 8 扇区（各 45°，坎=北扇区…）+ 中宫
   （定义：距太极点 < 0.2×等效半径 的区域为中宫）。
   - 房间归属 = 房间多边形质心所在扇区；
   - 跨扇区大房间：按**面积占比**输出主/次扇区（采样网格法，房间内均匀采样 ≥200 点按扇区计数）。
3. **二十四山**：`facing_deg` → 24 山查表（每山 15°，子=352.5–7.5°…）。
   **临界角【硬约束】**：若 facing 距山界 < tolerance(5°) → 返回两个候选坐向，前端强制提示复测，**不静默择一**。
4. **缺角判定**：外轮廓的最小外接矩形 → 8 扇区理论面积；某扇区内实有宅体面积 < 理论值×0.6 → 判缺角（阈值可配）。异形宅(轮廓顶点>10 或凹陷>2 处)输出 `confidence:"low"` 提示人工确认。
5. **穿堂/门对门视线**：门中点沿 facing 方向作视线线段，与房间多边形求交；
   大门视线未被任何内墙/家具多边形截断即达对侧门窗 → 穿堂；两 room 门中线距离 < 阈值且 facing 相对(夹角>165°) → 门对门。

## 2.4 玄学叠加（overlay.py）

- `xuankong(period, sitting)` 的 `九宫{卦:{运,山,向}}` 按扇区贴到平面 → 每房间得飞星组合；
- `bazhai(year,gender)` 的八游年贴扇区 → 床/灶/书桌/厕归位判定（用 room.type + bed_head_deg / stove_mouth_deg）；
- 运(period)缺省取当前九运(2024–2043)，可传建宅年份推。

## 2.5 诊断输出（report.py）→ FloorplanDiag

```json
{
  "taiji": [x,y], "sitting":"子", "facing":"午", "sitting_candidates":["子"],
  "rooms":[{"id":"r1","type":"master_bedroom","sector":"坤",
            "flying_star":{"运":4,"山":2,"向":6}, "bazhai_younian":"绝命",
            "issues":[{"code":"star_2","desc":"二黑病符临卧室"},
                      {"code":"younian_jue","desc":"床位压绝命方"}]}],
  "sha":[{"code":"chuantang","desc":"大门与阳台门一线","rooms":["entry","living"],
          "confidence":"high"}],
  "que_jiao":[{"sector":"乾","ratio":0.41,"confidence":"high"}]
}
```
此结构同时是：① P1 `prescribe(floorplan=...)` 的输入；② 前端图上标注的渲染源；③ LLM 报告的事实层。

## 2.6 API

```
POST /api/floorplan/diagnose     { floorplan: Floorplan, birth?: Birth, period?: int }
→ FloorplanDiag
POST /api/floorplan/report       { diag: FloorplanDiag, birth?: Birth }
→ SSE 流：LLM 家居风水报告（事实只来自 diag + prescribe 处方；口径走红线）
```

## 2.7 前端（追加到《前端设计计划书》§5.13，本方案为权威版）

- **标注画布**：上传户型图为底图(仅描摹参考) → 画外轮廓 → 框房间选类型 → 摆门(拖转 facing) → 标梁(可选)。Konva.js / SVG 实现；所有标注可撤销重做、可保存重载。
- **朝向输入**：手机罗盘 API(`deviceorientationabsolute`) + 手动度数双通道；提示"远离金属/电器测量"；临界角弹复测提示。
- **结果视图**：平面图叠 8 扇区色块(按吉凶淡染) + 各房间角标飞星三数/游年名 + 问题点红点 → 点击出"问题→化解(来自 P1)"卡片；右侧 LLM 报告流式。
- **导出**：标注图 + 报告合成 PDF。

## 2.8 P2 验收清单

- [ ] geometry 单测：方形宅质心居中；L 形宅缺角扇区正确；旋转 facing 后扇区跟转
- [ ] 2–3 个标准户型(方形/L形/刀把形)人工叠九宫 vs 程序归属一致率 100%（主扇区）
- [ ] 临界角(如 6.5°，子癸界 7.5°±5 内)→ 返回双候选 + 复测提示，绝不静默择一
- [ ] 同输入重算结果逐字节稳定（确定性）
- [ ] 五黄/二黑落卧室用例 → diag.issues 正确 → prescribe 出金泄化解且无带货词
- [ ] 穿堂/门对门/厕居中宫各 1 个构造用例检出；1 个不应误报的用例不误报
- [ ] LLM 报告中每条断言可回溯到 diag 字段（抽查），无 diag 外杜撰
- [ ] 红线全检：报告无恐吓措辞、无购买高价物、medium 化解带"咨询专业"

---

# 第三部分 · 排期与里程碑

| 周 | 交付 |
|----|------|
| W1 | P1 知识库四表(带 source) + Remedy 模型 + priority 红线过滤 + 单测 |
| W2 | prescribe 引擎 + 解读层接入 + /api/remedy + P1 验收全过 |
| W3 | P2 models+geometry(质心/扇区/24山/缺角) + 全部几何单测 |
| W4 | overlay 叠加 + sha_detect + FloorplanDiag + /api/floorplan/diagnose |
| W5 | 前端标注画布 + 朝向输入 + 结果视图 |
| W6 | LLM 报告(流式) + PDF 导出 + P2 验收全过 + 文档(文献依据增补) |

**铁律不变**：P1 先行——平面图的"怎么改"全部复用 P1 知识库；W3 开始前 P1 验收必须全绿。

---

# 第四部分 · 风险登记（实现级）

| 风险 | 缓解 |
|------|------|
| 知识库口诀流派分歧（如三碧化解用火泄还是静制） | 每条标 source，分歧条目双记录并陈，默认取主流 |
| 凹形宅质心落到宅外 | 检测 point-in-polygon，落外则要求手动定太极点 |
| 扇区法 vs 九宫格法结果差异（业界两派都有） | 【已决策】扇区法为准（圆周等分更合罗盘本义）；文档注明 |
| 罗盘 API 精度差/部分机型无 absolute | 双通道输入兜底；±5° 容差 + 临界复测是硬约束 |
| 化解被 LLM 加码（自创"再买个…更好"） | 系统提示锁死"仅可来自处方清单"+输出禁词扫描双保险 |
| 用户把化解当医疗/财务方案 | 处方与报告均带象征性定位语 + 专业转介（红线） |
